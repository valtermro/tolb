/**
 * Converts a given string to upper case.
 *
 * @function
 * @param {string} str - The string to convert
 * @return {string} `str` converted to upper case
 * @example
 *
 *   toUpper('foo') //=> 'FOO'
 */
export default function toUpper(str) {
  return str.toUpperCase();
}
