/**
 * Converts a given string to lower case.
 *
 * @function
 * @param {string} str - The string to convert
 * @return {string} `str` converted to lower case
 * @example
 *
 *   toLower('FOO') //=> 'foo'
 */
export default function toLower(str) {
  return str.toLowerCase()
}
